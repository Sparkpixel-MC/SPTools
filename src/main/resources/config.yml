# 队列系统配置

# 队列检查间隔（秒）
queue_check_interval: 5

# 玩家确认超时时间（秒）
confirm_timeout: 30

# 队列配置示例
queues:
  survival:
    min_players: 2
    max_players: 10
    auto_start: true
  creative:
    min_players: 1
    max_players: 5
    auto_start: false